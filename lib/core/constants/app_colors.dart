import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF3498DB);
  static const Color primaryDark = Color(0xFF2980B9);

  // Background Colors
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
  static const Color textLight = Color(0xFF95A5A6);

  // Success Colors
  static const Color success = Color(0xFF27AE60);

  // Drawer Colors
  static const Color drawerHeaderStart = Color(0xFF3498DB);
  static const Color drawerHeaderEnd = Color(0xFF2980B9);

  // Shadow Colors
  static Color shadowLight = Colors.grey.withOpacity(0.1);
}
